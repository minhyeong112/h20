# For more information, see the Configuration Guide :
# https://www.librechat.ai/docs/configuration/librechat_yaml

# Configuration version (required)
version: 1.2.8
# This setting caches the config file for faster loading across app lifecycle
cache: false

# Custom interface configuration
interface:
  # Enable fork functionality
  showFork: true
  
  # Disable presets
  endpointsMenu: false
  
  # Privacy Policy and Terms of Service
  privacyPolicy:
    externalUrl: "https://tryvajra.com/privacy"
    openNewTab: true
  termsOfService:
    externalUrl: "https://tryvajra.com/terms"
    openNewTab: true
  
  # Remove parameters in right menu
  parameters: false

# Definition of custom endpoints
endpoints:
  # Enable agents and assistants
  assistants:
    disableBuilder: false
    pollIntervalMs: 750
    timeoutMs: 180000
    capabilities: ["code_interpreter", "retrieval", "actions", "tools", "image_vision"]
    
  agents:
    disableBuilder: false
    pollIntervalMs: 750  
    timeoutMs: 180000
    
  custom:
    # XAI (Grok)
    - name: 'XAI'
      apiKey: '${XAI_API_KEY}'
      baseURL: 'https://api.x.ai/v1'
      models:
        default: ['grok-4', 'grok-3']
        fetch: false
      titleConvo: true
      titleModel: 'grok-3'
      modelDisplayLabel: 'XAI'

    # Perplexity
    - name: 'Perplexity'
      apiKey: '${PERPLEXITY_API_KEY}'
      baseURL: 'https://api.perplexity.ai'
      models:
        default: ['sonar-deep-research', 'sonar-reasoning-pro', 'sonar-reasoning', 'sonar-pro', 'sonar']
        fetch: false
      titleConvo: true
      titleModel: 'sonar'
      modelDisplayLabel: 'Perplexity'

# Web Search Configuration
webSearch:
  # Search Provider Configuration
  serperApiKey: "${SERPER_API_KEY}"
  searchProvider: "serper"
  
  # Scraper Configuration
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  scraperType: "firecrawl"
  
  # Reranker Configuration
  jinaApiKey: "${JINA_API_KEY}"
  rerankerType: "jina"
  
  # General Settings
  scraperTimeout: 7500
  safeSearch: 1
  
  # Enable web search by default (users can still toggle)
  defaultEnabled: true

# Speech Configuration
speech:
  # Default user settings for speech tab
  speechTab:
    conversationMode: true
    advancedMode: false
    speechToText:
      engineSTT: "external"
      languageSTT: "English (US)"
      autoTranscribeAudio: true
      decibelValue: -45
      autoSendText: -1
    textToSpeech:
      engineTTS: "external"
      voice: "alloy"
      languageTTS: "en"
      automaticPlayback: true
      playbackRate: 1.0
      cacheTTS: true

  # Speech-to-Text Configuration (OpenAI Whisper)
  stt:
    openai:
      apiKey: '${OPENAI_API_KEY}'
      model: 'whisper-1'

  # Text-to-Speech Configuration (ElevenLabs)
  #tts:
  #  elevenlabs:
  #    apiKey: '${TTS_API_KEY}'
  #    model: 'eleven_multilingual_v2'
  #    voices: ['202898wioas09d2', 'addwqr324tesfsf', '3asdasr3qrq44w', 'adsadsa']
  tts:
    openai:
      apiKey: '${OPENAI_API_KEY}'
      model: 'tts-1'
      voices: ['alloy', 'echo', 'fable', 'onyx', 'nova', 'shimmer']